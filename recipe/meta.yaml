{% set name = "pygamma_n" %}
{% set version = "1.0.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://currents.soest.hawaii.edu/hgstage/pygamma/archive/v{{ version }}.tar.gz
  sha256: c719ac3f467509933fffd93acc1dc95f964a726b5b9e704bd053281c8441e749

build:
  number: 2
  skip: true  # [win]
  script:
    - export SETUPTOOLS_SCM_PRETEND_VERSION={{ version }}  # [not win]
    - set SETUPTOOLS_SCM_PRETEND_VERSION={{ version }}    # [win]
    - {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  build:
    - {{ stdlib('c') }}
    - {{ compiler('c') }}
    - {{ compiler('fortran') }}
  host:
    - python
    - pip
    - numpy >=2
    - meson-python
    - netcdf-fortran
    - setuptools_scm
  run:
    - python
    - netcdf-fortran
    - numpy

test:
  source_files:
    - pygamma_n/tests/
  requires:
   - pytest
   - pip
  imports:
    - pygamma_n
  commands:
    - pip check
    - pytest pygamma_n/tests/

about:
  home: https://currents.soest.hawaii.edu/hgstage/pygamma
  license: MIT AND LicenseRef-CSIRO
  license_file: pygamma_n/src/ConditionsOfUse
  summary: Python interface for the original Fortran neutral density

extra:
  recipe-maintainers:
    - ocefpaf
    - efiring
